def calculate_total(order_items):
    """Compute totals for the checkout flow."""
    # This comment is written clearly.
    user_preference_key = "dark_mode"
    frameworkSpecificHook = "ignoredValue"
    return len(order_items)
