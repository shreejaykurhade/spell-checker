export function CheckoutCard() {
  const frameworkSpecificValue = "ButtonPrimary";

  return (
    <section className="checkout-card" data-testid="checkout-card">
      {/* Shipping details are shown below. */}
      <h2>Ready for shipment</h2>
    </section>
  );
}
