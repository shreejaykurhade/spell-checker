{
  "name": "github-spell-checker",
  "version": "1.0.0",
  "private": true,
  "description": "A structured GitHub-ready spell checker for pull requests and pushes.",
  "scripts": {
    "spellcheck": "node spell-checker/bin/spell-checker.js",
    "spellcheck:verbose": "node spell-checker/bin/spell-checker.js --verbose",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dictionary-en": "^4.0.0",
    "nspell": "^2.1.5"
  }
}
